using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MamaManager : MonoBehaviour
{
    // currentState = searching, returning, turningOnLight
    // currentRoom
    // isAlerted 
    // Every time the player triggers a noise event, add the lightSwitch to the stack. Mama will turn them off in the order these events were triggered. 
}
