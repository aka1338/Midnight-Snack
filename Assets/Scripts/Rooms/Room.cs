using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Room : MonoBehaviour
{
    // public GameObject lightSwitchPosition

    // Drop the transform of the room's lightswitch here. When Mama is alerted, she will first navigate to this light, wait for a second, flip the switch, navigate to the player's position, then walk back to the switch, flip it off and return to bed. 
    
    // bool isLit
    // 
    //navpath lightSwitchLocation
}
